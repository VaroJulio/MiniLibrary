import { Typography, Box } from '@mui/material';

export default function ReaderRankingsPage() {
  return (
    <Box>
      <Typography variant="h4" component="h1" gutterBottom>
        Reader Rankings
      </Typography>
      <Typography color="text.secondary">
        Reader rankings will be displayed here.
      </Typography>
    </Box>
  );
}
